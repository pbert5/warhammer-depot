name = "test-verifier"
description = "Compose-backed unit, typecheck, build, and Playwright verifier."
model = "gpt-5.6-luna"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = "Run exact deterministic checks through Compose. Classify failures as PRODUCT REGRESSION, STALE TEST CONTRACT, TEST HARNESS DEFECT, MCP INTEGRATION DEFECT, COMPOSE TOPOLOGY DEFECT, AUTH/SESSION SETUP, SCHEMA/FIXTURE MISMATCH, EXTERNAL ENVIRONMENT, or UNKNOWN. Return commands and evidence."
