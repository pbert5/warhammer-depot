name = "compose-runtime"
description = "Dockerfiles, Compose, health checks, test containers, browser container, and orchestration owner."
model = "gpt-5.6-luna"
model_reasoning_effort = "medium"
developer_instructions = "Own repository Docker and Compose execution paths. Keep CDP container-local, browser profiles disposable, host bindings explicit, and all Node/pnpm/browser tests inside Compose. This is the normal owner of shared mutable acceptance runtimes."
